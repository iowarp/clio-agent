# CLIO/GACT Provider Selector Polish Tasks

## Open Issues

- [x] No-guard cross-file routing could still fail after a correct planner attempt. ALCF/gpt-oss selected/attempted `analysis` for the four-file triage, but `_expert_file_compatibility_error()` rejected the analysis coordinator because the current-file context held only the first HDF5 path and ignored `coordinated_file_suffixes`. The compatibility check now evaluates question + file context together and allows registered coordinator experts for natural multi-file bundles. Evidence: first 14-case demo run failed `reasoning_cross_file_triage_nanoagents` with repeated compatibility errors; focused unit coverage added; rerun passed with `route_source=dspy`, selected `analysis`, six tool calls, and four tier-3 child sessions.
- [x] Natural HDF5 dataset prompts required tool-shaped wording. A prompt like "Focus on plasma/electron_temperature..." should call `hdf5_analyze_dataset` without the user naming the tool. DataExpert now treats named dataset paths plus natural focus/chunk/statistics language as dataset-level analysis. Evidence: `test_natural_dataset_focus_uses_dataset_tool` and ALCF demo case `hdf5_dataset_focus` passed with `hdf5_analyze_dataset`.
- [x] Memory demo prompt was too weak and allowed a chat answer with no fresh evidence. The demo runner now asks CLIO to compute schema/statistics while relying on the prior Parquet path, producing a real memory + tool-use case. Evidence: first run failed `workflow_memory_followup` as `chat` with no tools; rerun passed as `analysis` with `parquet_analyze_schema` plus five `parquet_compute_statistics` calls.
- [x] ALCF/gpt-oss benchmark exposed that the planner could use `shell_bash` as a scientific-file shortcut before falling back to native CSV tools. In GACT this created a 120s permission wait and poisoned the otherwise-correct CSV answer with `error_info=tool_error`. CLIO now rejects non-explicit shell actions for scientific file prompts before tool execution, ignores compiled `[Available Tools]` text when deciding whether the user explicitly asked for shell, and keeps shell available for utility diagnostics such as time/date. Evidence: initial ALCF CSV turn called denied `shell_bash` then `csv_read_table` and failed the benchmark; focused unit tests pass; live manual CSV turn used only `csv_read_table` with `stop_reason=end_turn`; focused ALCF workflow now passes in 51.07s.
- [x] ALCF command-line model discovery still depended on `/resource_server/{cluster}/jobs`, which can time out even when the documented endpoint catalog and inference path work. `scripts/list_alcf_models.py` now uses `/resource_server/list-endpoints` as the authoritative catalog and treats `/jobs` as optional live-status annotation only. Evidence: initial `uv run python scripts/list_alcf_models.py --json` timed out on `/jobs`; direct `list-endpoints` returned HTTP 200 with Sophia/Metis catalogs.
- [x] ALCF Metis can validate token/catalog and pass raw inference, but a real GACT `/messages` turn with `argonne/openai/gpt-oss-120b` stalled and left the conversation with `(no parts)` / session `error`. Root cause: LiteLLM's OpenAI path builds telemetry headers via `platform.platform()`, which can block inside Python 3.12's Windows WMI probe before any provider timeout applies. CLIO's Windows platform hardening now covers `platform.platform()` / `win32_ver`, not only `platform.uname()`. Evidence: direct LiteLLM/Metis completion returned `Paris is the capital of France.` in 1.46s; redeployed `myclio`, selected ALCF Metis `gpt-oss-120b`, and a real GACT `/messages` turn returned assistant text `Paris is the capital of France.` with `error_info=null` and session `idle` on poll 2.
- [x] ALCF Sophia Save+Connect could send the local no-auth placeholder API key `x` as a real bearer token, producing `Token introspection: Token is either not active or invalid` immediately after selecting the provider. The TUI now only sends placeholder API keys for local no-auth OpenAI-compatible providers, never OAuth/CLI providers; the CLIO backend also treats empty/`x`/`EMPTY` as placeholders for Argonne and resolves a real Globus token defensively. ALCF provider save now returns quickly as `state=configuring`, and Argonne turns refresh the short-lived Globus token into live DSPy LM kwargs before execution. Evidence: live Sophia `PUT /v1/providers/lm` returned in 0.233s as `configuring`, reached `ready`, and `what is the capital of france?` returned `The capital of France is Paris...` with `error_info=null`.
- [x] Status text overflows or truncates despite available space. Long transport errors and provider status messages now wrap across multiple lines in the Selected panel.
- [x] Long API bases truncate instead of wrapping cleanly. ALCF URLs and custom local endpoints now hard-wrap without ellipses.
- [x] Remove all remaining "ALCF local vLLM (compute-node)" / compute-node-specific local-vLLM wording. The provider is simply "vLLM (localhost)" in current source and verified screenshots.
- [x] Make LM Studio reachable on initial provider check, without requiring Ctrl+R after the modal opens.
- [x] Verify Ollama live detection against the running local service and surface the real reason if it is not usable. Current machine: Ollama is reachable but has no installed models.
- [x] Fix Codex and Claude Code model catalogs. Static candidate catalogs show reliably without a live `/models` endpoint.
- [x] Confirm Claude Code/Codex model selection semantics and do not present lack of scriptable live model discovery as lack of model selection.
- [x] Remove jarring black row backgrounds inside the modal. Text rows now use the modal background consistently.
- [x] Add model filtering/search inside the Model section.
- [x] Sort model catalogs alphabetically before rendering/selecting.
- [x] Add a model-details surface when providers expose useful metadata, and document when context/window size is not discoverable.
- [x] Treat LM Studio context length as a load-time setting, not a completion token limit. The backend now reads LM Studio native model metadata and applies requested context length through LM Studio's native load API before swapping CLIO's global LM.
- [x] Keep provider-specific model knobs honest. LM Studio shows context length; Codex/Claude Code hide unsupported numeric tuning; temperature/max output remain request-time settings.
- [x] Stop stale per-session ModelRefs from breaking global provider swaps. New/duplicated TUI sessions no longer carry default Anthropic model refs, and backend provider save clears stale session model refs.
- [x] Make ALCF auth action wording stateful. The selector shows Authenticate when no token is ready and Refresh token when Globus auth is already ready.
- [x] Fix Settings Agent tab decode failure when `/v1/agents` returns `default_model` as a string. The TUI wire type now accepts both legacy string model ids and structured `ModelRef` objects.
- [x] Fix incongruent context display. When a provider does not report model context but the user has configured a context length, the TUI now reports the configured context instead of saying "not reported".
- [x] Stale model refs still trigger `501 session model overrides` after changing model/provider and sending the next message. The backend heals stale session refs when a global LM is configured, the TUI now clears cached session ModelRefs after Save, and a live LM Studio/Qwopus probe returned `POST /messages` 200 rather than 501 after provider save.
- [x] Provider/model changes must be applied at Save time, not at next Send time. Saving now loads/reuses the selected LM during `PUT /v1/providers/lm`, clears backend and TUI session refs immediately, mirrors global provider info locally, and queues a session refresh before the next send.
- [x] LM Studio provider save can fail while the requested model is already loaded. Live repro: `/api/v1/models` reported five loaded `qwopus3.5-9b-v3` instances at 32768 context, but `PUT /v1/providers/lm` called `/api/v1/models/load` anyway and surfaced `model_load_failed`. CLIO now reuses an already-loaded LM Studio instance when its context matches the requested context, and only calls the load endpoint when a load or context change is actually required.
- [x] Real LM Studio/Qwopus turn can remain `running` after a successful provider save and accepted `POST /messages`. Live repro after the stale-model fixes: `PUT /v1/providers/lm` returned 200, stale session model cleared, `POST /messages` returned 200, but after 45 seconds no assistant message existed and the session was still `running`.
  - Fixed by adding `CLIO_GACT_TURN_TIMEOUT_S` watchdog around GACT turn execution. On timeout, CLIO now emits/persists an assistant `provider_timeout` error, sets the session to `error`, preserves partial streamed text if any, and labels executor cancellation as `best_effort` / `executor_work_may_continue=true` instead of hiding the failure or leaving the turn running forever. The no-agent path now returns structured `503 agent_not_available` before the watchdog.
  - Evidence: live LM Studio/Qwopus probe with `CLIO_GACT_TURN_TIMEOUT_S=5` returned provider save 200 and post 200, then persisted an assistant `error_info.error=provider_timeout`, `stop_reason=error`, and session `status=error` instead of remaining `running`.
- [x] Make ALCF Refresh token actually force the backend auth flow. The ready-token button now sends `force=true`; Authenticate still uses the normal first-login/check path.
- [x] ALCF auth readiness must not punt validation to the user when a cached Globus token exists. The polite provider-selector path now validates/refreshes through Globus noninteractively via `check_auth_status()` and reports `ready`/`Globus token validated` when that succeeds; stored-but-unrefreshable tokens surface as `auth_required` with explicit re-auth wording. The heavier service-side availability check remains the model catalog's `/resource_server/{cluster}/jobs` probe. Focused evidence: `test_get_lm_provider_reports_argonne_valid_token_ready`, `test_get_lm_provider_reports_argonne_refresh_failure`, and a live-code `/v1/providers/lm` probe reporting Sophia/Metis `ready`.
- [x] ALCF provider selector still looked broken after auth was fixed because the Sophia `/jobs` catalog probe used a brittle 4-second read timeout, cached the timeout as `unavailable`, and disabled model selection even though the token and inference path worked. The ALCF jobs probe now uses a 12-second budget; focused TUI render coverage asserts ready ALCF renders `auth: Globus token ready` / `status: ready`, and VHS screenshot `gact-tui/screenshots/clio_lm_config_alcf_sophia.png` verifies Sophia shows live models with `Save and connect` enabled instead of login-required/unavailable state.
- [x] ALCF model catalog source was too narrow. Docs expose `/resource_server/list-endpoints` for model/endpoint discovery and `/{cluster}/jobs` for live/queued status. The provider picker now uses `list-endpoints` for selectable ALCF models and overlays `/jobs` only for live-status descriptions. Evidence: `test_argonne_model_catalog_uses_list_endpoints_and_jobs_for_status` plus live `list-endpoints` returning Sophia/Metis model catalogs.
- [x] ALCF provider Save could mix preset state because Sophia and Metis share provider kind `argonne`. The TUI preserved current provider config whenever the provider kind matched, so choosing Metis after Sophia could still send Sophia's API base. Preset switching now preserves current config only when the preset API base matches too. Evidence: `TestLMConfigPresetSwitchUsesPresetAPIBaseForSameProviderKind`.
- [x] TUI default Save omitted max output, but the backend request schema filled the omission with `32000`, bypassing `LMProviderConfig`'s provider default and making ALCF/gpt-oss turns vulnerable to huge output/timeouts. `LMProviderRequest.max_tokens` now uses `0` as the provider-default sentinel. Evidence: `test_put_argonne_uses_provider_default_max_tokens_when_omitted`.
- [x] ALCF auth refresh is currently attempted inside the noninteractive backend request thread. Globus then hits EOF waiting for an authorization code. Current backend returns interactive launch/manual instructions from `/v1/providers/{provider_id}/auth` instead of running the code prompt hidden in the server request thread. Focused evidence: `test_auth_provider_returns_interactive_argonne_instructions`.
- [x] ALCF token readiness must be validated against real inference, not only local token files or `/jobs` metadata. Verified live Sophia inference with Bearer auth against `/resource_server/sophia/vllm/v1/chat/completions` using `meta-llama/Meta-Llama-3.1-8B-Instruct`; HTTP 200 returned `chat.completion`, `ALCF_TOKEN_OK`, and token usage.
- [x] Settings Agent tab still has wire-shape drift: CLIO returns `AgentDef.parameters` as an object, while the TUI shared type expects `[]AgentParameter`. Current `gact-tui develop` now tolerates both shapes via `AgentDef.UnmarshalJSON`; focused evidence: `go test .\emulator\pkg\gact -run "TestAgentDef" -count=1`.
- [x] Model configuration keyboard navigation is incomplete. Once focus is on Temperature/Max output/Context length/Thinking budget, Up/Down should move between rows in that panel and Left/Right should adjust the focused value. Current `gact-tui develop` behavior is covered by `TestLMConfigAdvancedRowsUseVerticalNavigation`.
- [x] Sending a message before any executable CLIO agent is ready must surface a specific `agent_not_available` error, not fall through to the turn watchdog or a generic config error. The response now distinguishes agent startup still in progress, startup failure, and no agent configured, and does not persist a user message or mark the session running. Focused evidence: `uv run pytest tests/test_gact/test_post_messages.py -k "without_agent or agent_starts or agent_start_failure or turn_timeout"` and `uv run ruff check src/clio_agent/gact/app.py tests/test_gact/test_post_messages.py`.
- [x] Header/top bar semantics are confusing on load. It can show both a stale per-session model (`model: claude-opus-4-7`) and the active global CLIO model (`model: Meta-Llama-3.1-8B-Instruct`), plus opaque labels like `ws: default`, `agent: default`, and raw backend URL `http://127.0.0.1:...`. Header now accepts a deployment label, spells out `workspace:`, suppresses default/no-op agent labels, and treats the configured global CLIO LM as authoritative over stale per-session model refs.
  - Progress target: change the TUI header contract so connected deployments can pass a display label (`name (type)`), workspace is spelled out, default/no-op agent labels are suppressed, and the global configured LM is treated as authoritative over stale per-session model refs. Add focused render tests and screenshot evidence before marking done.
  - Evidence: focused Go render tests passed, and VHS screenshot `screenshots/header-semantics.png` shows `myclio (clio)` with `workspace: default` instead of the raw URL / `ws:` label.
- [x] Windows VHS helper left `$GACT_BACKEND` literal after rewriting tapes from bash to `cmd`, causing screenshots to connect to `"$GACT_BACKEND/v1/capabilities"`. The helper now substitutes `$GACT_BACKEND` and `$GACT_BACKEND_LABEL` from the process environment before running VHS.
- [x] Tool-call observability is ambiguous in conversation. The assistant may say it called tools, but the TUI does not clearly show tool call events, arguments, results, or failures. `gact-tui` now merges `message.completed.metadata` into the live message cache and renders assistant `metadata.tools_called` as an explicit `Tool evidence` block when no structured `tool_call` transcript exists. The block labels itself as summary metadata, includes tool name, args, result preview, ok/error, duration/cache, and telemetry source, and avoids duplicating evidence when real `tool_call` parts are already visible. Fixed by `gact-tui` PR #40. Focused evidence: `go test .\tui\internal\ui -run "TestRenderAssistantToolEvidenceFromMetadata|TestStructuredToolCallsDoNotRenderDuplicateToolEvidence|TestMessageCompletedMergesToolEvidenceMetadata|TestRenderPartsForRoleWithResults_Interleaves|TestRenderPart_ToolCallClaudeCodeShape" -count=1`, `go build -o tui\gact.exe .\tui`, and `go test .\emulator\pkg\gact -count=1`.
- [ ] CLIO still sends tool-call telemetry as completed-turn metadata instead of live `tool_call` / `tool_result` parts during execution. Current `gact-tui` working tree promotes that metadata into structured parts on load/completion so finished conversations render in execution order and body-cursor/Ctrl+E can target the tool call/result, but this is not a true live transcript. Backend follow-up: emit first-class tool call/result events from the CLIO GACT turn loop as tools start/finish, preserving args, result, ok/error, duration, cache status, and telemetry source without waiting for `message.completed`.
- [x] Windows VHS screenshot workflow can still hang on attached-session TUI captures. Repro during tool-evidence visual verification: direct `vhs tmp\vhs_echo.tape` also timed out without a PNG, while the repo wrapper succeeded with pinned local ttyd. `gact-tui` PR #41 hardened `scripts/vhs-windows.ps1` so bash-authored `Type "VAR=value VAR2=value command"` lines are rewritten into Windows `set VAR=value&& set VAR2=value&& command` form. Evidence: wrapper completed `tmp\vhs_echo.tape`, completed a multi-env `VHS_ENV_OK` tape, and completed the real `tui\screenshot_per_part_cursor.tape` multi-tool TUI capture; inspected screenshots `TTTTTTTTT1_body_cursor_on_second_read.png`, `TTTTTTTTT1_ctrl_e_first_read.png`, and `UUUUUUUUU1_unified_diff_inline.png`.
- [x] `gact-tui` full `tui/internal/ui` package tests were failing on stale golden snapshots. Repro: `go test .\tui\internal\ui -count=1` expected old `ws: default` output while current `develop` rendered `workspace: default`. Fixed by `gact-tui` PR #42 after reviewing the diff as seven header-only snapshot changes. Evidence: `go test .\tui\internal\ui -update-views -count=1` and `go test .\tui\internal\ui -count=1`.
- [x] TUI send-failure hints were rendering structured backend errors as raw `gact: <status> <code>: ...` strings. `gact-tui` PR #43 now preserves structured backend error details in the client and renders `agent_not_available` as a direct user hint for starting/failed/not-configured states while keeping the unsent message available for retry. Evidence: `go test .\tui\internal\client -count=1`, `go test .\tui\internal\ui -count=1`, and `go build -o tui\gact.exe .\tui`.
- [x] TUI lacked an internationalization/localization architecture. `gact-tui` PR #44 added an embedded locale catalog (`locale/en.json`), locale normalization through `GACT_LOCALE`, English fallback semantics, named interpolation, and converted the post-send failure hint path to message IDs. Remaining caveat at the time: most TUI strings were still inline and needed incremental migration plus Unicode-width/non-Latin visual coverage. Evidence: `go test .\tui\internal\ui -run "TestLocalizer|TestPostFailed|TestPostMessageCmd" -count=1`, `go test .\tui\internal\ui -count=1`, and `go build -o tui\gact.exe .\tui`.
- [x] The new i18n foundation lacked a non-Latin catalog and rendered-path proof. `gact-tui` PR #45 added a Japanese catalog for the converted post-send failure messages and tests that configured Japanese locale selection produces Unicode text without falling back to English. Evidence: `go test .\tui\internal\ui -run "TestLocalizer|TestPostFailedJapanese" -count=1`, `go test .\tui\internal\ui -count=1`, and `go build -o tui\gact.exe .\tui`.
- [x] Finish the TUI language feature as an exposed user-facing setting, not only an env-var/i18n foundation. Added English, Spanish (machine), and Japanese (machine) catalogs; added a Settings > Language tab with live preview, persisted `locale` config, `GACT_LOCALE` resolution, and explicit machine-translation labels; localized the settings modal chrome/language surface and post-send error hints; added catalog key-parity coverage so future localized message IDs cannot silently exist only in English; and verified non-Latin rendering through Japanese tests and screenshots. Evidence: `go test -p 1 .\tui\internal\ui -run "TestLocalizer|TestPostFailedJapanese|TestSettings_.*Language|TestSettings_TabCycle|TestSettings_ShiftTab|TestSettings_ThemeAndLanguage" -count=1`, `go test -p 1 .\tui\internal\ui .\tui\internal\config -count=1`, `go test -p 1 .\tui -run "TestCLI_Env|TestEmitConfig|TestDiag|TestHelp" -count=1`, `go build -p 1 -o tui\gact.exe .\tui`, and VHS screenshots `gact-tui/screenshots/settings_language_es.png` and `gact-tui/screenshots/settings_language_ja.png`.
- [x] Provider selector short-viewport layout hides the primary `Save and connect` action while the footer still says Enter can save. `gact-tui` PR #46 reserves save-button space in shorter LM provider modal layouts and adds a 24-row regression test so the primary action remains visible without exceeding terminal height. Evidence: `go test .\tui\internal\ui -run "TestLMConfigShortModalKeepsSaveActionVisible|TestLMConfigModalHeightDoesNotExceedTerminal|TestLMConfigLayoutRespondsToTerminalHeight" -count=1`, `go test .\tui\internal\ui -count=1`, `go build -o tui\gact.exe .\tui`, and VHS screenshot `screenshots/clio_lm_config_small.png` regenerated at 1200x560 against a temporary CLIO backend showing the 2x2 grid and visible `Save and connect` action.
- [x] LM Studio model catalog sorting can silently change the pending model selection when no configured/suggested model is available. Live repro: `/v1/providers/lm_studio/models` returned `qwopus3.5-9b-v3` first, then `ibm/granite-4-h-tiny`; the TUI sorted alphabetically and selected `ibm/granite-4-h-tiny` in the provider modal. `gact-tui` PR #47 keeps catalogs sorted for display while preserving the provider's first returned live model as the default selection when CLIO has no configured/suggested model. Evidence: `go test .\tui\internal\ui -run "TestLMConfigLiveCatalogPreservesProviderDefaultBeforeSorting|TestLMConfigModelFilterUsesTypedTextAndCatalogIsSorted|TestLMConfigFallbackCatalogDoesNotOverwriteModel" -count=1`, `go test .\tui\internal\ui -count=1`, `go build -o tui\gact.exe .\tui`, and VHS `screenshots/clio_lm_config.png` showing sorted LM Studio models with `qwopus3.5-9b-v3` selected.
- [x] Empty conversation view shows hardcoded Go-code demo prompts (`read main.go`, `delete the temp dir`, `propose an edit to main.go`, etc.) even when attached to CLIO. Visual repro: `screenshot_settings_tabs` run against CLIO after closing the LM modal shows these fixed prompts in the conversation pane. `gact-tui` PR #48 removed emulator-specific starter prompts from the generic empty conversation pane, removed `/scenarios` and the hardcoded Scenarios help tab from generic help, and added a regression test for the old prompt strings. Evidence: `go test .\tui\internal\ui -update-views -run "TestView_ReadyWithSessions|TestView_HelpOverlay" -count=1`, `GOMAXPROCS=2 go test -p 1 .\tui\internal\ui -count=1`, and `GOMAXPROCS=2 go build -p 1 -o tui\gact.exe .\tui`. Caveat: a live VHS screenshot attempt against the temporary CLIO backend hung in the Windows ttyd/VHS layer after the code change, so verification used deterministic render-golden coverage rather than a fresh PNG.
- [x] Windows VHS wrapper can hang long enough to outlive the calling command and leave `vhs.exe` running. Repro: `scripts\vhs-windows.ps1 tmp\screenshot_clio_empty_conversation.tape -Backend http://127.0.0.1:18181` exceeded the shell timeout, produced no PNG, and left a `vhs.exe` process that had to be killed manually. `gact-tui` PR #49 added a wrapper-owned `-TimeoutSeconds` option, runs `vhs` as an owned child process, kills it on timeout, and propagates nonzero exits. Evidence: quick tape through `scripts\vhs-windows.ps1 tmp\vhs-ok.tape -TimeoutSeconds 30` completed; timeout tape through `scripts\vhs-windows.ps1 tmp\vhs-timeout.tape -TimeoutSeconds 1` failed with `vhs timed out after 1 second(s)`; `Get-Process` confirmed no orphan `vhs.exe` remained.
- [ ] `gact agent deploy clio` can kill CLIO before it binds `/v1/capabilities` on a cold Windows start. Live repro while restoring `myclio`: direct `clio_agent.gact.app` import took more than 20 seconds, but the deploy path kills CLIO after a hardcoded 10-second readiness budget and leaves the registry pointing at a dead agent. This must be fixed as a deploy-readiness problem with explicit diagnostics/configuration, not allowed to surface later as a generic message timeout.
- [x] CLIO GACT startup imports the full scientific tool gateway before binding `/v1/capabilities`. Root-cause trace: importing `clio_agent.gact.app` imports `clio_agent.tools.file_policy`, Python executes `clio_agent.tools.__init__`, that eagerly imports `gateway`, which imports every server including HDF5; on this Windows machine `h5py` blocks in `platform._wmi_query`. Fixed by making `clio_agent.tools` lazy, removing scientific/signature imports from `/v1/agents` catalog generation, and adding regression coverage that GACT/catalog imports do not load HDF5/HDF5 server modules.
- [x] Python 3.12 on this Windows workstation can hang inside `platform._wmi_query` during DSPy/LiteLLM/aiohttp/HDF5 imports. Fixed by seeding CLIO's Windows platform cache before CLIO-triggered DSPy imports and avoiding root test `conftest.py` scientific imports for non-scientific tests. Evidence: direct `import clio_agent; import dspy` and `import clio_agent; import clio_agent.agent` probes now complete, and focused GACT provider/catalog tests no longer hang at collection.
- [x] When a CLIO backend starts with no LM configured, the TUI header omits model/provider state and the provider selector does not appear automatically or through an obvious prompt. Backend timeout/root-cause fixes restored the intended auto-open path: a fresh unconfigured `clio-unconfigured-test` deployment returned `/v1/providers/lm configured=false`, and VHS screenshot `gact-tui/screenshots/clio_unconfigured_56883.png` shows the provider selector open on first screen with LM Studio selected and `Save and connect` visible.
- [ ] Local LM Studio/Qwopus semantic smoke after async provider swap ignored an exact-output prompt (`Reply with exactly CLIO_ASYNC_PROVIDER_OK and nothing else.`) and returned generic scientific-assistant readiness text. This did not surface as a provider error and did not appear to come from a literal string in `src/`, so it should be treated as planner/routing prompt semantics to harden, not transport readiness. Evidence: live `myclio` session `sess_06f6290ce561`, user message `msg_user_521afe64d2ad`, assistant `stop_reason=end_turn`, `error_info=None`.
- [x] `PUT /v1/providers/lm` can block the FastAPI event loop while applying LM Studio native load/context settings. Live repro: saving LM Studio/Qwopus timed out after 90s, then `/v1/capabilities`, `/v1/health`, and `/v1/providers/lm` stopped responding, which prevents the TUI provider selector from opening. Fixed by making LM Studio provider save start a tracked background operation, exposing `state=configuring|ready|error` on `/v1/providers/lm`, returning structured `provider_configuring` for message sends during the swap, and keeping health/capabilities/agent/provider endpoints responsive while the load runs.
- [x] TUI provider Save treated an asynchronous backend provider swap as immediate success, then overcorrected by keeping the modal open and swallowing keys while the backend loaded the model. Current behavior: Save closes the modal once the backend accepts `state=configuring`, the TUI polls `/v1/providers/lm` in the background until `ready` or `error`, and the user can keep typing while the model loads. Evidence: focused Go test `TestLMConfigAsyncSavePollsUntilReady`, full `tui/internal/ui` package test, client package test, and rebuilt `tui/gact.exe`.
- [x] Provider selector Tab semantics were still wrong inside Model configuration. Up/Down now moves between Temperature/Max output/Load context rows; Tab/Shift+Tab moves between section anchors instead of stepping through each configuration row. Evidence: `TestLMConfigTabMovesBetweenSectionsNotAdvancedRows` and `TestLMConfigAdvancedRowsUseVerticalNavigation`.
- [x] LM Studio context labels were ambiguous. The editable row now says `Load context`, while model details separately show `Max context` and `Requested load context`. Evidence: `TestLMConfigContextLabelsDistinguishMaxFromRequestedLoad` and screenshot `gact-tui/screenshots/clio_lm_config_current.png`.
- [x] LM Studio Save could still time out from stale synchronous provider-loading paths. Current backend returns `state=configuring` immediately and keeps capabilities/provider endpoints responsive while LM Studio load runs in the background. Evidence: rebuilt `gact.exe`, redeployed `myclio`, live `PUT /v1/providers/lm` returned in 58 ms with `state=configuring`, then reached `ready` after 7 seconds; focused async-save TUI test still passes. Caveat: the VHS save tape exposed key-timing/focus issues in the tape script, so the visual save proof is not used as evidence.
- [ ] LM Studio can accumulate many loaded model instances. Partial fix: CLIO now records an LM Studio instance as owned only when CLIO itself calls `/api/v1/models/load` and receives an `instance_id`, reuses already-loaded user instances without claiming ownership, and unloads CLIO-owned instances on backend shutdown through `/api/v1/models/unload`. Remaining work: unload CLIO-owned instances on successful provider replacement away from LM Studio without risking inconsistent provider state if the replacement fails.
- [x] ALCF Authenticate in the provider selector could block inside a stale-token status probe and then surface a backend request timeout. The auth endpoint no longer calls `check_auth_status()` in the button path; it launches the interactive Globus terminal flow immediately on Windows, returns quickly, and includes a manual command fallback in the instructions. The TUI auth request timeout is now 10 seconds and the transient message says it is launching login rather than checking a token. Follow-up fix: Windows now launches PowerShell with `-NoExit` plus a final `Read-Host`, so a failed/fast auth helper leaves a readable terminal instead of flashing closed. Evidence: `test_auth_provider_returns_interactive_argonne_instructions`.
- [x] ALCF model-unavailable messages in the provider selector could truncate the real token instruction (`Globus token stored; validate or refresh...`) in the Model panel. The Model panel now wraps provider-unavailable details across rows instead of rendering one clipped line. Evidence: `TestLMConfigModelUnavailableWarningWraps`.
- [x] TUI sometimes showed a full-screen `Connection error` when changing model/provider or switching session because an intentional SSE stream cancellation was surfaced as `sse: connect ... context canceled`. The TUI now converts pre-header `context.Canceled` from `startSSECmd` into an internal no-op transition instead of `StageError`, while real SSE connection/read failures still use the existing error/reconnect paths. Evidence: `TestSSEOpenCanceledDoesNotEnterErrorStage`, full `tui/internal/ui` package tests, and rebuilt `tui/gact.exe`.
## Verification Required

- [x] Focused Go TUI tests.
- [x] Focused Python provider tests.
- [x] Focused post-message stale-model regression tests.
- [x] Focused GACT wire decode test for string/object `default_model`.
- [x] Live LM Studio probe.
- [x] Live LM Studio native context metadata probe: Qwopus reports 262144 context tokens on this machine.
- [x] Live Ollama probe.
- [x] VHS screenshots for default provider selector, ALCF, Codex, Claude Code, and short-viewport views.
- [x] Restart or redeploy the user's CLIO backend/TUI session so labels come from current code instead of the old process on port 61671. Current `myclio` was redeployed on `127.0.0.1:56882` from the working tree after the async provider/auth fixes.
- [x] TUI conversation history did not visibly mark provider/model swaps, so users could not tell which turns happened before or after a global LM change. `gact-tui` now inserts a local muted dashed divider when a provider/model save reaches `ready`; reloaded history also gets a divider when persisted message `ModelRef`s change; backend-provided marker metadata renders the same way without normal `SYSTEM`/`(no parts)` noise. Evidence: `go test -p 1 .\tui\internal\ui -run "TestModelSwap|TestConversationRendersPersistedModelRefChangeDivider|TestLMConfigSavedMirrorsGlobalProviderAndClearsSessionModels|TestLMConfigAsyncSavePollsUntilReady" -count=1`, `go test -p 1 .\tui\internal\ui -count=1`, `go build -p 1 -o tui\gact.exe .\tui`, and VHS screenshot `gact-tui/screenshots/model_swap_marker.png`.
- [ ] Swapping between ALCF providers/models works as standalone conversational endpoints but appears to destabilize the active session after repeated provider/model changes. Need live repro, provider-state trace, and fix without hiding failures.
- [x] GACT TUI tests repeatedly triggered Windows Firewall because several e2e helpers built `emulator-server.exe` under fresh `%TEMP%` directories. Fixed in `gact-tui`: affected TUI/client/emulator e2e tests now build emulator binaries under stable `.tools/test-bin/*emulator-server*.exe` paths, so Windows sees stable executable identities instead of a new temp path each run.
- [ ] Localization coverage is partial: many visible TUI strings remain hardcoded, including intro/load text, provider selector, top/bottom bars, and modal chrome. Track extracted strings and add tests that fail on newly hardcoded user-facing text in converted surfaces.
  - 2026-05-22: Converted high-visibility `gact-tui` chrome to cataloged strings for `en`, `es`, and `ja`: connection screen, connection error title/backend label, header workspace/session/model/agent/routing labels, footer command labels/focus label, and provider-modal title/intro/loading/save/hint strings. Added render tests for Spanish header/footer and Japanese provider modal. Still open for sidebar/body/help/palette/file picker/doctor/catalog/provider rows and broader hardcoded-string tracking.
  - 2026-05-22 hardcoded-string audit for `gact-tui/tui/internal/ui`: this is not a future-nice-to-have; the current language feature is still visibly partial. Remaining extraction work should migrate these surfaces to message IDs in `i18n.go`, add `en/es/ja` entries, and add render tests that prove Spanish and Japanese do not fall back to English.
    - Main app chrome in `app.go`: intro splash hint `press any key to continue`; input placeholder `type a message ...`; connection-error action labels `retry now`, `quit`, and auto-retry text; transient hints such as `model: ...`, installed/removed MCP messages, applied-file messages, cleared-message messages, and status snippets like `current:`, `session empty`, `messages`, `status:`, `nothing running`.
    - Sidebar in `app.go`: `SESSIONS`, detached/busy title variants, `no sessions`, `to create`, `filter:`, `(no matches)`, fallback session title `untitled`, scroll markers, `CONTEXT`, `(no files)`, active/archived counters, context-file mode/path labels, and raw session status words.
    - Conversation pane in `app.go`: `CONVERSATION`; waiting-permission banner wording; empty-state/onboarding copy for sidebar actions, command palette/help, detach/reattach, first-message prompt, attach-file/compose/settings/theme hints; detached-session resume hint; `CLIO is thinking...`; pasted-content placeholder; model/provider swap divider labels if these are intended to be language-aware.
    - Command palette and search palette in `app.go`: modal titles `Commands` and `Search messages`; `filter:`/`query:` labels; no-match/loading/empty-query copy; run/jump/close key hints; palette current-value descriptions; all builtin command titles/descriptions for `/metrics`, `/theme`, `/theme-export`, `/mcp`, `/tools`, `/catalog`, `/skills`, `/agents-list`, `/mode`, `/clear`, `/copy`, `/diff`, `/compact`, and `Doctor`.
    - Help overlay in `app.go`: `Keybindings` title; tab names `Global`, `Sidebar`, `Conversation`, `Input`, `Commands`, `Permission`; every shortcut description; close/switch-tab hint. This is one of the most visible untranslated surfaces because it advertises the product's UI semantics.
    - Provider/model selector in `lm_config.go`: despite localized title/intro/fetch/save/hints, most body text remains hardcoded: section headers `Provider`, `Selected`, `Model`, `Model configuration`, `Model details`; `Save and connect`; status/auth phrases; `Checking provider catalog...`; provider-unavailable/no-catalog/no-models/no-match messages; filter labels; editable labels `API base`, `API key`, `Temperature`, `Max output`, `Load context`, `Thinking budget`; value text `backend default`, `provider default`, `(empty)`, `Managed by this provider`, adjustment hints, and model-detail labels such as `Name`, `Max context`, `Requested load context`, quantization/parameter source text.
    - Settings modal in `settings.go`: only part of the tab chrome/language tab is localized. Remaining strings include Model tab fallback `(unset)`, `Change provider...`, model-picker explanation; Agent loading state; Theme descriptions and preview/commit hints; TUI tab title `Display preferences`; row labels/descriptions for collapse threshold, cost warning/danger tokens, paste compression, intro splash, mouse controls; `on`/`off`, `lines`, `skip splash`, `show splash`; runtime-state block labels (`backend URL`, `voice cmd`, `theme`, `AltScreen`) and the footer explaining left/right/enter behavior.
    - Quit/compose/rename/context/workspace/file modals: `quit_confirm.go` (`Close the TUI?`, explanatory text, action labels, key hints); `compose.go` (`Compose`, subtitle, commit/cancel hints); `rename.go` (`Rename session`, save/cancel/move hints); `context_add.go` (`Add file to context`, mode/save/remove hints); `workspace_switch.go` (`Switch workspace`, empty list text, `(current)`, switch/cancel hints, `already on`, `switched to`); `file_picker.go` (`Insert file reference`, loading/no-match/filter/current-directory and close/select hints).
    - Catalog/doctor/metrics/MCP management modals: `catalog_browser.go` loading/error/disabled/status/hint text; `doctor.go` title `Doctor - Backend Health`, tabs, capability scorecard labels, table headers, `supported`/`missing`, bucket names, health statuses, retry/close hints; `metrics_view.go` title and section labels `Sessions`, `Messages`, `Tokens`, `Cost`, latency headings, loading/error/refresh/close hints; `mcp_manage.go` install/remove titles, error/loading/progress states, and install/remove key hints.
    - Message rendering in `render.go`: assistant/message-part labels are still English-only: `(no parts)`, `Tool evidence`, metadata explanation, `result:`, `(error)`, thinking label, routed/heuristic/confidence labels, collapsed-output hint, diff header and apply/reject status/hints, subagent start/done labels, history-compacted label, generic `[type]`, `(no changes)`, and fallback tool label `Tool`.
    - Non-render but still user-visible/error text in `voice.go`, `archive.go`, `workspace_switch.go`, `settings.go`, and command handlers should be classified as either protocol/debug text or localized user copy. Anything shown in `transientHint`, modal body, footer, sidebar, or conversation should be cataloged.
    - Required guardrail: add a lightweight hardcoded-string audit for converted render surfaces. At minimum, tests should render the sidebar, conversation empty state, help overlay, palette, provider selector body, settings tabs, quit modal, doctor/metrics, and representative message parts under `es` and `ja`, and assert known English sentinel phrases are absent unless they come from backend/user data.
  - 2026-05-22 second pass: the gap is larger than `tui/internal/ui`. The locale catalogs currently contain only 52 keys per language, so the feature is a partial UI skin, not application-wide localization.
    - CLI/manual surface in `gact-tui/tui/main.go`: `gact --help`, `gact man`, `gact version`, `gact diag`, `gact emit-config`, and most subcommands print hardcoded English usage, explanations, table headers, warnings, and errors. Examples include deploy/connect lifecycle messages, `agent list`, `session`, `tasks`, `hooks`, `plugins`, `theme`, `env`, `dashboard`, `detached`, `log`, `run`, `send`, `wait`, `tail`, `ping`, `export/import`, and all `usage:`/`unknown command` errors. Decide whether CLI localization is in scope; if yes, it needs a separate catalog layer from Bubbletea render strings.
    - Man-page text in `tui/main.go` is hardcoded separately from `printUsage()`. If CLI localization is supported, the manual cannot remain an English-only raw string that diverges from the command catalog.
    - Adapter/emulator-visible catalog text: `emulator/internal/server/handlers_catalog.go`, emulator scenarios, and adapter command metadata include English command titles/descriptions and `/help` output. Some of this is demo/test data, but the TUI renders backend command titles/descriptions directly in the palette, so the contract needs an owner: either backend-provided strings are locale-neutral/protocol data, or the TUI must request/render localized metadata.
    - Detail/browser overlays missed in the first pass: `detail_view.go` renders titles such as `<role> text - N lines`, line-range text, and scroll/page/close hints; `catalog_browser.go` renders navigation/drill/install/delete/back hints; both are visible modal chrome and should be cataloged with the other overlays.
    - Golden snapshot debt: `tui/internal/ui/testdata/*.golden` still expects English for `SESSIONS`, `CONVERSATION`, `no sessions`, `Keybindings`, `Commands`, and `(no messages yet...)`. These tests can mask English regressions because they only assert the English baseline. Add locale-specific golden/render tests rather than only updating the English snapshots.
    - Help-overlay data model problem: `helpTabs` is a literal English data table in `app.go` containing both tab names and descriptions. This should become key-based data (`titleID`, `descriptionID`) or a small localized keybinding model; otherwise every language change requires editing Go literals.
    - Builtin command palette data model problem: `paletteMatches()` builds builtin `gact.Command` values with English `Title` and `Description` literals. These should become localized at render/build time or separated from backend-provided command metadata so local commands are translatable without changing protocol data.
    - Status/error taxonomy problem: raw backend status words (`running`, `idle`, `waiting_permission`, `error`), provider states (`ready`, `missing_key`, `unavailable`, `configuring`), and source labels (`builtin`, `backend-dependent`, catalog/status source text) leak directly into user copy. Define which are protocol enums and map them to localized display labels at the boundary.
    - Unicode-width/encoding verification is still insufficient. The Japanese catalog proves a few paths render non-Latin text, but wide-character strings need visual/regression coverage in dense panes: sidebar counts, provider 2x2 grid, help tabs, settings rows, table headers, and modal footers. This should include tests at narrow heights/widths because wrapping differs for CJK text.
- [x] Conversation mouse-wheel/scroll behavior cannot reliably reach the true bottom after scrolling up, leaving the latest messages inaccessible or visually clipped. Fixed in `gact-tui`: Bubbletea mouse mode is enabled, wheel-up/down update conversation scroll state, and focused UI tests verify wheel-down returns to true sticky bottom.
  - 2026-05-22 follow-up: The same bottom-anchor bug existed for keyboard body navigation: pressing Down at the final navigable part did not reattach if the user had scrolled up first. Fixed in `gact-tui` by making Down-at-end restore sticky bottom. Also added configurable mouse support (`mouse_enabled`, default true): wheel events respect the setting, left-click changes focus between panes, and clicking a visible sidebar session selects it. Focused tests and full UI package tests pass.
- [x] CLIO lacks an obvious minimal local shell/bash tool exposed through the normal tool path for simple real tool-use demos such as current time. Added policy-aware `shell_bash`, mounted it through the normal tool gateway/GACT catalog, guarded it through the destructive permission path, and verified it with focused tests plus a direct `Get-Date` gateway smoke.
- [ ] Chat is still not a full scoped ReAct/tool loop. Until it has real tool execution, CLIO strips `[Available Tools]` from direct chat synthesis context so the model cannot pretend to call hidden tools. Target behavior: chat should run a ReAct loop like other experts, receive only chat-visible utility tools, route utility calls through planner/tool telemetry, and render those calls/results visibly in GACT instead of relying on unverified assistant claims.
- [x] `/skills` in GACT TUI showed `(no skills on this backend)` because CLIO only discovered flat `.claude/skills/*.md` files, not the Codex and `.agents` directory-based `SKILL.md` layouts used on this machine. CLIO now discovers Claude flat skills, Claude directory skills, Codex skills, and `.agents` skills as `AgentDef(source="skill")`; project-local skills override global ones; minimal skills get fallback search keywords. GACT TUI already filters `/v1/agents` by `source=skill`, so no TUI protocol change was needed. Evidence: `uv run pytest tests/test_gact/test_agent_write.py tests/test_gact/test_agents_catalog.py -q`, `go test -p 1 .\tui\internal\ui -run "TestCatalog|Catalog|Skills" -count=1`, and a local `/v1/agents` probe found 20 skill agents.
