{
  "_comment": "MCP fleet memory budget for the standard acceptance load (3 concurrent claude-haiku sessions, real CTE, data-semantics blueprint). Measured 2026-07-14 (#929). RATCHET: these numbers only go DOWN — after an optimization lands (#930 slices), record the MEDIAN of >=3 runs (never a single noise trough); never raise them to make a regression pass. Enforced by scripts/mcp_mem_attribution.py --assert-budget (5% runtime tolerance). Scope: the whole server process tree PLUS any pre-existing external clio-core daemon (sampled explicitly), INCLUDING provider-CLI processes (claude SDK CLI).",
  "load": "3 concurrent claude-haiku sessions, real CTE, data-semantics blueprint",
  "peak_gb": 3.57,
  "final_gb": 3.57,
  "recorded": "2026-07-14",
  "campaign_targets": {"peak_gb": 1.8, "final_gb": 1.3}
}
