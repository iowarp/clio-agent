# GENERATED by scripts/gen_env_reference.py -- DO NOT EDIT.
#
# Committed base-layer defaults for clio-agent, loaded by
# clio_agent.conf.ConfigStore as the layer BELOW the environment and
# above the in-code default (which stays the operative fallback). Every
# key mirrors an in-code conf.resolve default; a drift test regenerates
# this file and fails CI if they diverge. Comment lines carry the env
# override name, the coercion type and the defining module. Regenerate
# with: uv run python scripts/gen_env_reference.py

# CLIO_MAX_CONCURRENT_AGENT_TASKS  (int)  <- src/clio_agent/gact/turn_spawn.py
agent_tasks.max_concurrent: 3

# CLIO_AGENT_DISABLE_DEFAULT_REGISTRY_BOOTSTRAP  (bool)  <- src/clio_agent/gact/agent_blueprints.py
agents.disable_default_registry_bootstrap: false

# CLIO_ARC_CACHE_CAPACITY  (int)  <- src/clio_agent/arc/memory.py
arc.cache_capacity: 1000

# CLIO_ARC_CLIO_CORE_DAEMON_RECYCLE  (bool)  <- src/clio_agent/arc/clio_core_daemon.py
arc.clio_core.daemon_recycle_enabled: false

# CLIO_ARC_CLIO_CORE_DAEMON_RSS_CRITICAL  (int)  <- src/clio_agent/arc/clio_core_daemon.py
arc.clio_core.daemon_rss_critical_bytes: 4294967296

# CLIO_ARC_CLIO_CORE_DAEMON_RSS_WARN  (int)  <- src/clio_agent/arc/clio_core_daemon.py
arc.clio_core.daemon_rss_warn_bytes: 1073741824

# CLIO_ARC_CLIO_CORE_LIVENESS_TTL_S  (float)  <- src/clio_agent/arc/clio_core_liveness.py
arc.clio_core.liveness_ttl_s: 3.0

# CLIO_CORE_PORT  (str)  <- src/clio_agent/arc/clio_core_liveness.py
# arc.core_port:  # default unset; resolved in code

# CLIO_ARC_CTE_DIR  (str)  <- src/clio_agent/arc/clio_core_config.py
# arc.cte.dir:  # default unset; resolved in code

# CLIO_ARC_CTE_DISK_WARN_FRACTION  (float)  <- src/clio_agent/arc/clio_core_config.py
arc.cte.disk_warn_fraction: 0.5

# CLIO_ARC_CTE_FILE_CAPACITY  (str)  <- src/clio_agent/arc/clio_core_config.py
arc.cte.file_capacity: "50GB"

# CLIO_ARC_CTE_RAM_CAPACITY  (str)  <- src/clio_agent/arc/clio_core_config.py
arc.cte.ram_capacity: "1GB"

# CLIO_ARC_EVENTS_CHUNK_SEGMENTS  (int)  <- src/clio_agent/arc/memory.py
arc.events_chunk_segments: 512

# CLIO_ARC_LIVENESS_BACKOFF_INITIAL_S  (float)  <- src/clio_agent/arc/rpc_liveness.py
arc.liveness.backoff_initial_s: 2.0

# CLIO_ARC_LIVENESS_BACKOFF_MAX_S  (float)  <- src/clio_agent/arc/rpc_liveness.py
arc.liveness.backoff_max_s: 15.0

# CLIO_ARC_LIVENESS_RETRIES  (int)  <- src/clio_agent/arc/rpc_liveness.py
arc.liveness.retries: 3

# CLIO_ARC_LIVENESS_STALL_AFTER_S  (float)  <- src/clio_agent/arc/rpc_liveness.py
arc.liveness.stall_after_s: 30.0

# CLIO_ARC_LSM_COMPACTION_THRESHOLD  (int)  <- src/clio_agent/arc/memory.py
arc.lsm_compaction_threshold: 5

# CLIO_ARC_LSM_MEMTABLE_SIZE  (int)  <- src/clio_agent/arc/memory.py
arc.lsm_memtable_size: 1000

# CLIO_SERVER_CONF  (str)  <- src/clio_agent/arc/clio_core_liveness.py
# arc.server_conf:  # default unset; resolved in code

# CLIO_ARC_STORE  (str)  <- src/clio_agent/arc/storage.py
arc.store: "cte"

# CLIO_ARC_STORE_CONFIG  (str)  <- src/clio_agent/arc/storage.py
# arc.store_config:  # default unset; resolved in code

# CLIO_ARTIFACT_CAS_BUDGET_BYTES  (int)  <- src/clio_agent/gact/artifacts/cas.py
artifacts.cas_budget_bytes: 536870912

# CLIO_ARTIFACT_CAS_MAX_FILE_BYTES  (int)  <- src/clio_agent/gact/artifacts/cas.py
artifacts.cas_max_file_bytes: 16777216

# CLIO_ARTIFACTS_EXPORT_LICENSE  (str)  <- src/clio_agent/gact/artifacts/export.py
artifacts.export_license: "NOASSERTION"

# CLIO_ARTIFACTS_HASH_MAX_FILE_BYTES  (int)  <- src/clio_agent/gact/artifacts/minting.py
artifacts.hash_max_file_bytes: 67108864

# CLIO_ARTIFACT_HASH_STAT_CACHE  (bool)  <- src/clio_agent/gact/artifacts/cas.py
artifacts.hash_stat_cache: false

# CLIO_ARTIFACTS_INSTRUMENT_ARG_MAX_BYTES  (int)  <- src/clio_agent/gact/artifacts/transforms.py
artifacts.instrument_arg_max_bytes: 2048

# CLIO_ARTIFACTS_INSTRUMENT_TOTAL_MAX_BYTES  (int)  <- src/clio_agent/gact/artifacts/transforms.py
artifacts.instrument_total_max_bytes: 16384

# CLIO_ARTIFACTS_LINEAGE_MAX_NODES  (int)  <- src/clio_agent/gact/artifacts/lineage.py
artifacts.lineage_max_nodes: 500

# CLIO_ARTIFACTS_PROPOSALS_BATCH_MAX  (int)  <- src/clio_agent/gact/artifacts/proposals.py
artifacts.proposals_batch_max: 32

# CLIO_ARTIFACTS_PROPOSALS_PER_TURN  (int)  <- src/clio_agent/gact/artifacts/proposals.py
artifacts.proposals_per_turn: 8

# CLIO_AUTOCOMPACT_PCT  (str)  <- src/clio_agent/gact/runtime/context_tokens.py
autocompact.pct: "0.85"

# CLIO_DUMP_UNPARSEABLE  (str)  <- src/clio_agent/lm/adapters.py
# debug.dump_unparseable:  # default unset; resolved in code

# CLIO_DEBUG  (str)  <- src/clio_agent/runtime/trace.py
debug.level: "low"

# CLIO_LOG_LM_RESPONSE  (bool)  <- src/clio_agent/runtime/trace.py
debug.lm_response: false

# CLIO_DEBUG_MEMPROF  (bool)  <- src/clio_agent/gact/diagnostics.py
debug.memprof: false

# CLIO_DEBUG_MEMPROF_FRAMES  (int)  <- src/clio_agent/gact/diagnostics.py
debug.memprof_frames: 20

# CLIO_DEBUG_MEMPROF_OUT  (str)  <- src/clio_agent/gact/diagnostics.py
# debug.memprof_out:  # default unset; resolved in code

# CLIO_DEBUG_ONLY  (list)  <- src/clio_agent/runtime/trace.py
# debug.only:  # computed at runtime: _no_only

# CLIO_SSE_EVENT_LOG  (str)  <- src/clio_agent/gact/routes/misc.py
# debug.sse_event_log:  # default unset; resolved in code

# CLIO_SSE_WIRE_TAP  (str)  <- src/clio_agent/gact/routes/misc.py
# debug.sse_wire_tap:  # default unset; resolved in code

# CLIO_STREAM_AUDIT_LOG  (str)  <- src/clio_agent/runtime/stream_audit.py
# debug.stream_audit_log:  # default unset; resolved in code

# CLIO_BLUEPRINT_REGISTRY_URL  (str)  <- src/clio_agent/gact/agent_blueprints.py
gact.blueprint_registry.url: "https://github.com/iowarp/clio-agent-marketplace.git"

# CLIO_GACT_CORS_ORIGINS  (str)  <- src/clio_agent/gact/app.py
# gact.cors.origins:  # default unset; resolved in code

# CLIO_LEDGER_COMMAND_AUDIT_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.command_audit.max: 2000

# CLIO_LEDGER_CONTEXT_FRAMES_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.context_frames.max: 200

# CLIO_LEDGER_MEMORY_TOOL_AUDIT_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.memory_tool_audit.max: 2000

# CLIO_LEDGER_PENDING_DIFFS_HARD  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.pending_diffs.hard: 1000

# CLIO_LEDGER_PENDING_DIFFS_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.pending_diffs.max: 500

# CLIO_LEDGER_PERMISSIONS_HARD  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.permissions.hard: 4000

# CLIO_LEDGER_PERMISSIONS_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.permissions.max: 2000

# CLIO_LEDGER_SHARED_TOKENS_HARD  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.shared_tokens.hard: 10000

# CLIO_LEDGER_SHARED_TOKENS_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.shared_tokens.max: 5000

# CLIO_LEDGER_TURN_ATTEMPTS_HARD  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.turn_attempts.hard: 4000

# CLIO_LEDGER_TURN_ATTEMPTS_MAX  (int)  <- src/clio_agent/gact/runtime/retention.py
gact.ledger_retention.turn_attempts.max: 2000

# CLIO_LIVE_EDGE_STREAMING  (bool)  <- src/clio_agent/gact/live_edge.py
gact.live_edge_streaming: false

# CLIO_RESIDENT_LEDGERS_TTL_S  (float)  <- src/clio_agent/gact/resident_ledgers.py
gact.resident_ledgers.idle_ttl_s: 1800.0

# CLIO_RESIDENT_LEDGERS_MAX_BYTES  (int)  <- src/clio_agent/gact/resident_ledgers.py
gact.resident_ledgers.max_bytes: 536870912

# CLIO_RESIDENT_LEDGERS_MAX  (int)  <- src/clio_agent/gact/resident_ledgers.py
gact.resident_ledgers.max_sessions: 512

# CLIO_HOOKS_ALLOW_MANAGED_ONLY  (bool)  <- src/clio_agent/gact/hooks/dispatcher.py
hooks.allow_managed_only: false

# CLIO_HOOKS_CONFIG  (str)  <- src/clio_agent/gact/hooks/dispatcher.py
# hooks.config:  # default unset; resolved in code

# CLIO_HOOKS_DEFER_TIMEOUT  (float)  <- src/clio_agent/gact/hooks/defer.py
hooks.defer_timeout: 86400.0

# CLIO_HOOKS_MANAGED_CONFIG  (str)  <- src/clio_agent/gact/hooks/dispatcher.py
# hooks.managed_config:  # default unset; resolved in code

# CLIO_HOOKS_STOP_LOOP_CAP  (int)  <- src/clio_agent/gact/hooks/stop_loop.py
hooks.stop_loop_cap: 8

# CLIO_HOOKS_TRUST_STORE  (str)  <- src/clio_agent/gact/hooks/dispatcher.py
# hooks.trust_store:  # default unset; resolved in code

# CLIO_CTX_MAX_BYTES  (int)  <- src/clio_agent/gact/runtime/constants.py
limits.context_inline_bytes: 32768

# CLIO_EXTRACT_REPAIR_ATTEMPTS  (float)  <- src/clio_agent/gact/agents/builders.py
limits.extract_repair_attempts: 3.0

# CLIO_FS_MAX_READ_BYTES  (int)  <- src/clio_agent/tools/servers/fs_server.py
limits.fs_read_bytes: 262144

# CLIO_MAX_LM_CALL_S  (float)  <- src/clio_agent/runtime/lm_activity.py
limits.lm_call_s: 1800.0

# CLIO_LM_INTER_TOKEN_IDLE_S  (float)  <- src/clio_agent/runtime/lm_activity.py
limits.lm_inter_token_idle_s: 120.0

# CLIO_LM_PARSE_RETRY_ATTEMPTS  (str)  <- src/clio_agent/lm/adapters.py
# limits.lm_parse_retry_attempts:  # default unset; resolved in code

# CLIO_LM_TRANSIENT_BACKOFF_S  (float)  <- src/clio_agent/lm/io_logging.py
limits.lm_transient_backoff_s: 8.0

# CLIO_LM_TRANSIENT_RETRIES  (float)  <- src/clio_agent/lm/io_logging.py
limits.lm_transient_retries: 2.0

# CLIO_GACT_MCP_RECONNECT_TIMEOUT_S  (float)  <- src/clio_agent/gact/routes/mcp.py
limits.mcp_reconnect_timeout_s: 15.0

# CLIO_SHELL_DEFAULT_OUTPUT_BYTES  (int)  <- src/clio_agent/tools/servers/shell_server.py
limits.shell_default_output_bytes: 16384

# CLIO_SHELL_DEFAULT_TIMEOUT_S  (float)  <- src/clio_agent/tools/servers/shell_server.py
limits.shell_default_timeout_s: 5.0

# CLIO_SHELL_MAX_COMMAND_CHARS  (int)  <- src/clio_agent/tools/servers/shell_server.py
limits.shell_max_command_chars: 4000

# CLIO_SHELL_MAX_OUTPUT_BYTES  (int)  <- src/clio_agent/tools/servers/shell_server.py
limits.shell_max_output_bytes: 131072

# CLIO_SHELL_MAX_TIMEOUT_S  (float)  <- src/clio_agent/tools/servers/shell_server.py
limits.shell_max_timeout_s: 30.0

# CLIO_SUBMIT_REPAIR_ATTEMPTS  (float)  <- src/clio_agent/gact/agents/reactv2.py
limits.submit_repair_attempts: 3.0

# CLIO_TRANSIENT_PROVIDER_RETRY_DELAYS  (list)  <- src/clio_agent/agent.py
# limits.transient_provider_retry_delays:  # default unset; resolved in code

# CLIO_GACT_TURN_TIMEOUT_S  (float)  <- src/clio_agent/gact/_params.py
limits.turn_timeout_s: 900.0

# CLIO_LM_API_BASE  (str)  <- src/clio_agent/config.py
# lm.api_base:  # default unset; resolved in code

# CLIO_CLAUDE_CODE_TRANSPORT  (str)  <- src/clio_agent/config.py
# lm.claude_code_transport:  # default unset; resolved in code

# CLIO_CODEX_TRANSPORT  (str)  <- src/clio_agent/config.py
# lm.codex_transport:  # default unset; resolved in code

# CLIO_LM_DEFER_TIKTOKEN  (bool)  <- src/clio_agent/lm/factory.py
lm.defer_tiktoken: true

# CLIO_DISABLE_JSON_ADAPTER_FALLBACK  (bool)  <- src/clio_agent/lm/adapters.py
lm.disable_json_adapter_fallback: false

# CLIO_LM_DISABLE_THINKING  (bool)  <- src/clio_agent/lm/factory.py
lm.disable_thinking: false

# CLIO_LM_GUIDED_OUTPUT  (bool)  <- src/clio_agent/lm/adapters.py
lm.guided_output: false

# CLIO_LMSTUDIO_FLASH_ATTENTION  (bool)  <- src/clio_agent/gact/routes/providers.py
lm.lmstudio_flash_attention: true

# CLIO_LM_MAX_TOKENS  (int)  <- src/clio_agent/config.py
# lm.max_tokens:  # default unset; resolved in code

# CLIO_LM_MIN_P  (float)  <- src/clio_agent/config.py
# lm.min_p:  # default unset; resolved in code

# CLIO_LM_MODEL  (str)  <- src/clio_agent/config.py
# lm.model:  # default unset; resolved in code

# CLIO_LM_PLANNER_MAX_TOKENS  (int)  <- src/clio_agent/config.py
# lm.planner_max_tokens:  # default unset; resolved in code

# CLIO_LM_PLANNER_TEMPERATURE  (float)  <- src/clio_agent/config.py
# lm.planner_temperature:  # default unset; resolved in code

# CLIO_LM_PRESENCE_PENALTY  (float)  <- src/clio_agent/config.py
# lm.presence_penalty:  # default unset; resolved in code

# CLIO_LM_PROVIDER  (str)  <- src/clio_agent/config.py
lm.provider: "lm_studio"

# CLIO_LM_REASONING_MODEL  (str)  <- src/clio_agent/lm/adapters.py
# lm.reasoning_model:  # default unset; resolved in code

# CLIO_LM_STOP_SEQUENCES  (str)  <- src/clio_agent/lm/factory.py
# lm.stop_sequences:  # default unset; resolved in code

# CLIO_LM_TEMPERATURE  (float)  <- src/clio_agent/config.py
# lm.temperature:  # default unset; resolved in code

# CLIO_LM_THINKING_BUDGET  (int)  <- src/clio_agent/config.py
# lm.thinking_budget:  # default unset; resolved in code

# CLIO_LM_THINKING_LEVEL  (str)  <- src/clio_agent/config.py
# lm.thinking_level:  # default unset; resolved in code

# CLIO_LM_TOP_K  (int)  <- src/clio_agent/config.py
# lm.top_k:  # default unset; resolved in code

# CLIO_LM_TOP_P  (float)  <- src/clio_agent/config.py
# lm.top_p:  # default unset; resolved in code

# CLIO_DATA_DIR  (str)  <- src/clio_agent/runtime/status.py
paths.data_dir: ".clio/agent"

# CLIO_MODEL_DB  (str)  <- src/clio_agent/providers/handshake/sources/db.py
# paths.model_db:  # default unset; resolved in code

# CLIO_SESSIONS_PATH  (str)  <- src/clio_agent/gact/sessions.py
# paths.sessions:  # default unset; resolved in code

# CLIO_WEB_DIR  (str)  <- src/clio_agent/gact/app.py
# paths.web_dir:  # default unset; resolved in code

# CLIO_AI_REVIEW_TIMEOUT_S  (str)  <- src/clio_agent/gact/runtime/ai_review.py
permissions.ai_review_timeout_s: "45.0"

# CLIO_CLAUDE_CODE_SESSION_REUSE  (bool)  <- src/clio_agent/providers/claude_code_sessions.py
providers.claude_code.session_reuse: true

# CLIO_CLAUDE_CODE_STATEFUL_CAPACITY  (float)  <- src/clio_agent/providers/claude_code_stateful.py
providers.claude_code.stateful_capacity: 128.0

# CLIO_CODEX_STATEFUL_CAPACITY  (float)  <- src/clio_agent/providers/codex_stateful.py
providers.codex.stateful_capacity: 128.0

# CLIO_API_BASE  (str)  <- src/clio_agent/runtime/status.py
# runtime.api_base:  # default unset; resolved in code

# CLIO_CAPTURE_REASONING  (bool)  <- src/clio_agent/gact/usage.py
runtime.capture_reasoning: true

# CLIO_ENVIRONMENT  (str)  <- src/clio_agent/config.py
runtime.environment: "dev"

# CLIO_LIVE_STREAMING  (bool)  <- src/clio_agent/lm/adapters.py
runtime.live_streaming: true

# CLIO_LM_TOKEN_LIVENESS  (bool)  <- src/clio_agent/lm/io_logging.py
runtime.lm_token_liveness: true

# CLIO_SANDBOX_ENABLED  (bool)  <- src/clio_agent/runtime/sandbox.py
sandbox.enabled: true

# CLIO_ALLOW_SYMLINKS  (bool)  <- src/clio_agent/tools/file_policy.py
tools.file_policy.allow_symlinks: false

# CLIO_ALLOWED_ROOTS  (str)  <- src/clio_agent/tools/file_policy.py
# tools.file_policy.allowed_roots:  # computed at runtime: _default_allowed_roots()

# CLIO_MAX_FILE_SIZE_BYTES  (str)  <- src/clio_agent/tools/file_policy.py
tools.file_policy.max_file_size_bytes: "1073741824"

# CLIO_MCP_LISTING_TTL_H  (float)  <- src/clio_agent/tools/listing_cache.py
tools.mcp.listing_ttl_h: 24.0

# CLIO_MCP_SETUP_TIMEOUT_S  (float)  <- src/clio_agent/tools/execution.py
tools.mcp.setup_timeout_s: 10.0

# CLIO_MCP_SPAWN_DIET  (bool)  <- src/clio_agent/tools/spawn_diet.py
tools.mcp.spawn_diet: true

# CLIO_MCP_SPAWN_DIET_TTL_H  (float)  <- src/clio_agent/tools/spawn_diet.py
tools.mcp.spawn_diet_ttl_h: 24.0

# CLIO_MCP_WORKSPACE_MAX_RESIDENT  (int)  <- src/clio_agent/tools/reaper.py
tools.mcp.workspace_max_resident: 2

# CLIO_MCP_WORKSPACE_TTL_S  (float)  <- src/clio_agent/tools/reaper.py
tools.mcp.workspace_ttl_s: 120.0

# CLIO_MCP_CACHE_MAX_AGE_DAYS  (float)  <- src/clio_agent/tools/mcp_cache.py
tools.mcp_cache.max_age_days: 14.0

# CLIO_MCP_CACHE_MAX_BYTES  (int)  <- src/clio_agent/tools/mcp_cache.py
tools.mcp_cache.max_bytes: 2147483648

# CLIO_MCP_CACHE_TEMP_MAX_AGE_DAYS  (float)  <- src/clio_agent/runtime/disk_gc.py
tools.mcp_cache.temp_max_age_days: 3.0

# CLIO_MCP_CACHE_TEMP_ROOTS  (str)  <- src/clio_agent/runtime/disk_gc.py
# tools.mcp_cache.temp_roots:  # default unset; resolved in code

# CLIO_WINDOWS_SHELL_BACKEND  (str)  <- src/clio_agent/tools/servers/shell_server.py
tools.shell.windows_backend: "powershell"

# CLIO_SEMANTIC_TRACE_BACKEND  (str)  <- src/clio_agent/arc/memory.py
trace.backend: "none"

# CLIO_SEMANTIC_TRACE_DETAIL  (str)  <- src/clio_agent/gact/_params.py
# trace.detail_level:  # computed at runtime: DEFAULT_DETAIL_LEVEL

# CLIO_SEMANTIC_TRACE_PATH  (str)  <- src/clio_agent/gact/semantic_events.py
# trace.path:  # default unset; resolved in code

# CLIO_SEMANTIC_TRACE_CONFIG  (str)  <- src/clio_agent/gact/semantic_events.py
# trace.semantic_config:  # default unset; resolved in code

# CLIO_SEMANTIC_TRACE_FACTORY  (str)  <- src/clio_agent/gact/semantic_events.py
# trace.semantic_factory:  # default unset; resolved in code

# CLIO_WORKFLOW_STEP_INACTIVITY_S  (float)  <- src/clio_agent/gact/workflow_step_watch.py
workflows.step_inactivity_s: 120.0
