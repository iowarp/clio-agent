# Second test catalog

One component: `SecondText` (a `Text` alias). Test fixture only -- distinct
from `a2ui_packs/minimal`'s catalog so two-blueprint isolation tests can
activate one pack per session.
