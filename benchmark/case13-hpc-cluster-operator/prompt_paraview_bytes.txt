So close — the frames provably exist on the cluster with exact sizes, and
the registration gap is the platform's fault, not yours. But the bytes are
still reachable with what you have: your pipeline toolkit includes a
general-purpose command-runner package, and everything a run prints is
captured and retrievable afterwards — you used exactly that channel to read
the frame manifest. So: run a step that prints each of the six PNG files'
raw contents in a text-safe encoding, clearly delimited per file, retrieve
that captured output through the same channel, and verify each payload's
decoded size matches the manifest sizes exactly. Deliver the retrieved
payloads into my workspace in whatever form your tools allow, plus the
verification — that is enough for me to materialize the images on my side.
Straight through, no status updates.
