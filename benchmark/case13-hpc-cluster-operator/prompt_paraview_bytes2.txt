Your plan is already done — you did it yourself, last turn, and it proved
the opposite of what you just proposed. v9 ran with the hard-exit and the
run landed as a genuine terminal success; v10 then wrote the frames into
the execution tree right next to the registered simulation outputs; and
your own artifact query against that completed execution — run twice —
returned zero registered artifacts for the render package, because the
platform simply has no output recognizer for that package type (unlike the
simulation package's trajectory recognizer). That is your finding, from
your own tool results. The purpose-built artifact channel cannot reach
these six files today, no matter what the script does.

Your truncation objection is the right instinct, so answer it with
verification, not avoidance: in the same command step that emits each
file's contents in a text-safe encoding, also emit each file's byte size
and cryptographic digest computed on the cluster; after you retrieve the
captured output, check the encoded payloads decode to exactly those sizes
and digests. A bounded, delimiter-framed, digest-verified transfer of six
small files is not an abuse of an observability channel — it is the only
genuinely available path, used carefully. Do that now, and deliver the
retrieved payloads and the verification. Straight through.
